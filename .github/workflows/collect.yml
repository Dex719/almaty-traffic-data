name: Collect traffic

on:
  schedule:
    - cron: "13,43 * * * *"
  workflow_dispatch:

# One running collector; a pending successor waits instead of skipping itself.
concurrency:
  group: traffic-collector
  cancel-in-progress: false

permissions:
  contents: write
  actions: write

jobs:
  shift:
    runs-on: ubuntu-latest
    timeout-minutes: 355
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 1
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
      - run: pip install --requirement requirements.txt
      - name: Configure publication identity
        run: |
          git config user.name "traffic-bot"
          git config user.email "traffic-bot@users.noreply.github.com"
      - name: Collect with shutdown margin
        run: python -m collector.shift --minutes 330 --git
      - name: Preserve recovery data on failure
        if: failure() || cancelled()
        uses: actions/upload-artifact@v4
        with:
          name: traffic-recovery-${{ github.run_id }}
          path: data/
          include-hidden-files: true
          retention-days: 14
      - name: Queue successor
        if: always() && !cancelled()
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh api -X POST "/repos/${{ github.repository }}/actions/workflows/collect.yml/dispatches" -f ref=main

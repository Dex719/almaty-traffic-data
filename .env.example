# Copy to /etc/almaty-traffic.env (mode 0600); never commit real values.
# Optional HTTPS dead-man-switch URL. No source data is sent.
TRAFFIC_HEARTBEAT_URL=

# Enable after configuring and testing the external backup mount.
TRAFFIC_REQUIRE_BACKUP=0
